# LTC Reader — Arduino Nano + MAX7219

Проект: декодер SMPTE LTC (Linear Timecode) из аудиосигнала с выводом таймкода
на 8-значный 7-сегментный индикатор MAX7219.

Статус: скетч написан полностью — `ltc_reader_nano/ltc_reader_nano.ino`
(9 блоков). Логика декодера, детектор sync word и раскладка полей кадра
(стандарт SMPTE 12M / libltc, LSB-first) проверены на всех трёх эталонных
WAV (25/24/30 fps) скриптом `tools/ltc_wav_check.py`. Осталось: компиляция в Arduino
IDE, прошивка (через ISP), проверка на стенде.

---

## 1. Что такое LTC (кратко)

LTC (SMPTE 12M) — таймкод, закодированный в аудио методом **bi-phase mark**
(разновидность манчестерского кодирования):

- **80 бит на кадр** видео; битовая скорость = fps × 80
- Переход сигнала есть в **начале каждого битового интервала**; бит **«1»** —
  дополнительный переход **в середине** интервала, бит **«0»** — без него
- Кадр завершается **sync word `0011111111111101`** (биты 64–79) — единственное
  место в потоке с 12 единицами подряд, по нему находится граница кадра
- Полезные данные: **HH:MM:SS:FF в BCD-коде** + 32 бита user bits + флаги
  (drop frame и др.)

### Тайминги по стандартам

| Параметр              | 24 fps     | 25 fps (EBU) | 29.97/30 fps |
|-----------------------|------------|--------------|--------------|
| Битовая скорость      | 1920 бит/с | 2000 бит/с   | 2397.6/2400 бит/с |
| Длительность бита     | 520.8 мкс  | 500 мкс      | 417 мкс      |
| Длительность полбита  | 260.4 мкс  | 250 мкс      | 208.3 мкс    |
| Период кадра          | 41.7 мс    | 40 мс        | 33.3 мс      |
| Макс. частота в сигнале | ~3.8 кГц | ~4 кГц       | ~4.8 кГц     |

Уровень входного сигнала: профессиональный +4 dBu (~3.5 Vpp) или
потребительский −10 dBV (~0.6 Vpp). Для детектора переходов через ноль
достаточно ~100 мВ.

## 2. Зафиксированные решения

| Вопрос        | Решение                                  |
|---------------|------------------------------------------|
| Индикатор     | MAX7219, 8 цифр (7-сегментный модуль)    |
| Частота кадров| Автоопределение 24/25/30 по периоду кадра|
| Вывод         | Полный TC: HH.MM.SS.FF (разделители — десятичные точки) |
| Захват сигнала| Аналоговый компаратор + Timer1 Input Capture (аппаратный) |

### Почему компаратор + Input Capture (а не альтернативы)

| Подход | Плюсы | Минусы |
|---|---|---|
| **А. Компаратор + Timer1 ICP (выбран)** | Аппаратный тайминг с точностью 0.5 мкс, ~1–2% CPU, помехоустойчивость (noise canceller) | Нужна обвязка смещения |
| Б. ADC-поллинг | Один аналоговый пин | Больше CPU, сложнее фильтрация |
| В. Библиотеки на CHANGE + micros() | Быстрый старт | Разрешение micros() 4 мкс + джиттер прерываний |

Нагрузка CPU (вариант А, худший случай — все единицы, 30 fps):
4800 фронтов/с × ~2 мкс ISR ≈ 1%.

Ресурсы: ~400 строк кода, ~6–8 КБ flash, < 200 байт RAM — с запасом на
ATmega328P (16 МГц, 2 КБ RAM, 32 КБ flash).

## 3. Схема

### Распределение пинов Nano

| Пин          | Назначение                                    |
|--------------|-----------------------------------------------|
| D7 (AIN1)    | Вход LTC (смещённый на Vcc/2)                 |
| D6 (AIN0)    | Опора Vcc/2 (делитель + bypass-конденсатор)   |
| D11 (MOSI)   | MAX7219 DIN (аппаратный SPI)                  |
| D13 (SCK)    | MAX7219 CLK                                   |
| D10 (CS)     | MAX7219 LOAD/CS                               |
| D8, D9, D12  | Свободны (кнопка режимов и т.п.)              |

Захват фронтов идёт **внутренне** через бит ACIC — пин ICP1 (D8) не нужен.

### Входная цепь (обвязка)

```
Аудио вход ── 0.47uF ── 10k ──┬──► D7 (AIN1)
                              │
                     100k ─┬──┴──┬─ 100k
                           │     │
                          GND   5V      (смещение на Vcc/2)
                              │
                     Vcc/2 ── 100nF ── GND (bypass)
                              │
                              └──► D6 (AIN0)  (опора, свой делитель 2×100k)
```

- Разделительный конденсатор 0.47 мкФ
- Резистор 10 кОм последовательно во вход
- Делитель 2 × 100 кОм — смещение входа на Vcc/2
- Опора на AIN0 — свой делитель 2 × 100 кОм + bypass 100 нФ
- Защитные диоды 2 × 1N4148 на входе (на Vcc и GND)
- RC-фильтр ~6 кГц (срез выше макс. частоты сигнала 4.8 кГц)

### Компоненты (BOM)

- Arduino Nano
- Модуль MAX7219 на 8 цифр 7-сегментный (не 8×8 матрица!)
- 4 × 100 кОм (два делителя), 10 кОм, 0.47 мкФ, 100 нФ
- 2 × 1N4148

## 4. Архитектура ПО

```
ISR (Timer1 CAPT) ──► кольцевой буфер uint16_t (метки фронтов ICR1)
        │
loop(): вычитка буфера
        │
Декодер bi-phase mark: классификация интервалов (короткий/длинный),
длинный → «0», пара коротких → «1», сдвиговый аккумулятор до 80 бит
        │
Детектор sync word 0011111111111101 → граница кадра + битовый индекс
        │
Парсер BCD: HH:MM:SS:FF, флаг drop frame
Авто-fps: интервал между sync words (40 мс → 25, 33.3 → 30, 41.7 → 24)
        │
Вывод на MAX7219 (прямой SPI, без библиотек): HH.MM.SS.FF
```

### Ключевые технические детали

- Timer1: prescaler 8 → тик 0.5 мкс; включить `ICNC1` (noise canceller)
  и `ACIC` (маршрутизация выхода компаратора на Input Capture)
- Переполнение Timer1 не страшно: интервалы < 600 мкс, беззнаковая разность
  корректна
- Порог классификации интервалов: ~0.75 битового периода (адаптивный)
- Антидребезг: игнор фронтов ближе ~80 мкс
- Обновление дисплея на каждый декодированный кадр; при потере сигнала
  > 500 мс — мигание/гашение
- Толерантность к varispeed, усреднение определения fps

## 5. Порядок реализации (этапы)

1. **Каркас скетча** — Timer1 + ISR захвата, кольцевой буфер
2. **Декодер bi-phase mark** — классификация интервалов, битовый аккумулятор
3. **Парсер кадра** — BCD, sync word, авто-fps
4. **Вывод на MAX7219** — прямой SPI, формат HH.MM.SS.FF
5. **Отладка** — Serial-лог кадров (115200), тест-сигнал
6. **Полировка** — антидребезг, varispeed, потеря сигнала

### Тестирование

- Генератор LTC: WAV с https://elteesee.pehrhovey.net (элтиси) с телефона/ПК
  или вторая Arduino в роли генератора LTC
- Проверить все fps: 24, 25, 30 (в т.ч. 29.97 drop frame)

## 6. План файла скетча

Один файл `ltc_reader_nano.ino`, без внешних библиотек, ~450 строк,
~180 байт RAM (буфер 128 Б + состояние ~50 Б). Разделы по порядку:

| # | Раздел | ~строк | Содержимое |
|---|--------|--------|-----------|
| 1 | Константы | 60 | Пины (CS=10, аппаратный SPI 11/13); `SYNC_WORD 0x3FFD`; антидребезг 80 мкс; порог 0.75 бит-периода; размер буфера 64; `#define DEBUG` |
| 2 | Глобальное состояние | 50 | Кольцевой буфер `volatile uint16_t edgeBuf[64]` + head/tail; 80-битный сдвиговый регистр + защёлка кадра (детектор sync word); адаптивный порог; `struct TimeCode {h,m,s,f,drop,user}`; период кадра для fps; `lastFrameMs` |
| 3 | setup() | 60 | Serial 115200; SPI master 4 МГц; MAX7219: scan limit 8, decode mode B (Code B — DP-бит работает, свой шрифт не нужен), intensity; Timer1: prescaler 8, `ICNC1`, `ICES1`; ACSR: `ACIC=1` (компаратор → ICP, прерывание компаратора не используем); `DIDR1` — отключить цифровые буферы AIN0/AIN1; стартовый экран `--------` |
| 4 | ISR(TIMER1_CAPT) | 20 | `edgeBuf[head++] = ICR1;` + toggle `ICES1` (захват обоих фронтов по очереди); ~15 инструкций |
| 5 | Декодер bi-phase mark | 70 | Вычитка буфера, беззнаковая `delta`; адаптивный порог (скользящее среднее коротких/длинных интервалов); длинный → «0», пара коротких → «1», короткий+длинный → ресинк; после каждого бита — проверка последних 16 бит на sync word → граница кадра |
| 6 | Парсер BCD | 60 | Разбор битовой карты (см. ниже), BCD → числа, валидация диапазонов (h<24, m/s<60, f<fps) |
| 6b | Фильтр правдоподобия | 30 | Монотонность таймкода: одиночный сбойный кадр не показываем (сбойная пара фронтов даёт валидный по BCD, но чужой TC). (Ре)захват после потери сигнала >500 мс или смены fps; смена источника — после 8 подряд «не туда» |
| 7 | Авто-fps | 40 | Период между sync words, усреднение ×8: 40.0 мс → 25; 33.33 → 30; 33.33/33.37 → 30 (29.97 по периоду неотличим — см. DF-флаг); 41.67 → 24; нестандартный период (varispeed) → fps не меняем. Замер на каждом готовом кадре (независимо от parseFrame); двойной интервал (потеря sync) отсеивается; смена скорости — после 4 подряд классификаций |
| 8 | MAX7219 драйвер | 60 | `max7219_send(reg, val)`: CS low → `SPI.transfer16` → CS high; `renderTC()`: 8 цифр Code B, точки на позициях 1/3/5 (HH.MM.SS.FF); мигание/гашение при потере сигнала |
| 9 | loop() | 40 | `processEdges()` → на новый кадр: парсинг, fps, вывод, debug-лог; потеря сигнала > 500 мс → мигание |

### Карта бит LTC-кадра (для парсера)

Стандарт SMPTE 12M (сверено с libltc): user bits вклиниваются между
нибблами времени — tens каждого BCD-поля лежит в СЛЕДУЮЩЕМ байте, а не
в старшем ниббле своего.

```
биты 0–3    кадры, единицы (BCD)
биты 4–7    user bits (UB1)
биты 8–9    кадры, десятки (BCD)
бит  10     флаг drop frame
бит  11     флаг color frame
биты 12–15  user bits (UB2)
биты 16–19  секунды, единицы (BCD)
биты 20–23  user bits (UB3)
биты 24–26  секунды, десятки (BCD)
бит  27     polarity correction
биты 28–31  user bits (UB4)
биты 32–35  минуты, единицы (BCD)
биты 36–39  user bits (UB5)
биты 40–42  минуты, десятки (BCD)
бит  43     BGF0
биты 44–47  user bits (UB6)
биты 48–51  часы, единицы (BCD)
биты 52–55  user bits (UB7)
биты 56–57  часы, десятки (BCD)
бит  58     BGF1
бит  59     BGF2
биты 60–63  user bits (UB8)
биты 64–79  sync word 0011111111111101 (0x3FFD)
```

### Принятые микро-решения

- Захват обоих фронтов: toggle `ICES1` в ISR (как в референсе ltc_reader)
- MAX7219 decode mode B (Code B): цифры 0–9 декодируются аппаратно,
  DP-бит работает — таблица шрифтов не нужна
- 29.97 vs 30: период кадра 33.367 vs 33.333 мс (различимо при усреднении
  ×8) + флаг DF из бита 6
- SPI 4 МГц (DIV4) — с запасом против максимума MAX7219 10 МГц

## 7. Риски и меры

| Риск                              | Мера                                          |
|-----------------------------------|-----------------------------------------------|
| Дребезг компаратора на шуме       | ICNC1 + программный гейт + RC-фильтр          |
| Очень слабый вход (< ~50 мВ)      | Внешний усилитель (транзистор/опамп) — предусмотреть место на плате |
| 29.97 DF vs 30 NDF                | Флаг drop frame + точное измерение периода кадра |
| MAX7219 матрица вместо 7-сегмента | Проверить модуль при покупке                  |

## 8. Референсы (существующие решения)

- **ltc_reader** (0x664b44c3) — https://github.com/0x664b44c3/ltc_reader —
  лёгкий LTC-декодер для AVR: компаратор + ICP, та же архитектура.
  Главный референс, в репо есть схема обвязки.
- **LTC-LinearTimecode-SMPTE** (CppPlayground) —
  https://github.com/CppPlayground/LTC-LinearTimecode-SMPTE — Arduino-библиотека
  на CHANGE-прерываниях (запасной вариант).
- **libltc** (x42 / Robin Gareus) — https://github.com/x42/libltc — эталонная
  библиотека, компилируется под AVR, но ориентирована на поток аудио-сэмплов;
  для 2 КБ RAM Nano тяжеловата и избыточна.
- Проекты для Teensy (ArtScienceLab/ARDUINO_LTC_DECODER, FrankBoesing) —
  не переносимы напрямую (audio shield, больше RAM).
- Спецификация: SMPTE 12M; описание — https://en.wikipedia.org/wiki/Linear_timecode
