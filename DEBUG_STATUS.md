# Отладка на стенде — статус

## 2026-09-21: раскладка полей исправлена ПОЛНОСТЬЮ (tens-нибблы)

Исправление 2026-09-02 нашло сдвиг на байт, но оказалось неполным:
оставались неверными ДЕСЯТКИ всех полей. Стандарт SMPTE 12M (сверено с
libltc): user bits вклиниваются между нибблами времени, поэтому tens
каждого BCD-поля лежит в СЛЕДУЮЩЕМ байте:

```
  b0 = кадры ед.;   b1[1:0] = кадры дес.; b1[2] = DF; b1[3] = CF
  b2 = секунды ед.; b3[2:0] = секунды дес.
  b4 = минуты ед.;  b5[2:0] = минуты дес.
  b6 = часы ед.;    b7[1:0] = часы дес.
```

Старый код читал tens из старшего ниббла своего же байта, а там user bits
(обычно 0) — поэтому у любого поля ≥ 10 десяток терялся. На стенде это не
было видно: проверялись моменты, где все поля < 10.

Проверено на всех трёх эталонных WAV (25/24/30 fps):

```
  t=12с   было 01:00:02:01  →  стало 01:00:12:01
  t=150с  было 01:02:00:01  →  стало 01:02:30:01
```

Последовательность кадров идёт ровно +1, время совпадает с реальным.

Исправлено: `parseFrame` в `ltc_reader_nano.ino`,
карта бит в `PLAN.md`, проверка в `tools/ltc_wav_check.py`.
**Нужно пересобрать hex и прошить (через ISP).**

Дополнительно (проверка на стенде, файл 24fps): дисплей **зеркалил разряды**
— верный TC `02:02:33:05` читался как `503.32.02.0`. Это, наоборот,
подтвердило, что декодер верен (все поля валидны). `renderTC` развёрнут:
`d[0]` → reg8.

### Ресинк после паузы (баг «залипания» на старом TC)

Симптом: остановить/перезапустить таймкод → дисплей навсегда моргает старым
TC, новый не показывается. Причина: при обрыве `haveEdge`/`lastEdge` остаются
старыми, и первый фронт после паузы даёт огромный интервал → фантомный бит,
сдвиг всего потока на 1 бит и порча адаптивных порогов (EMA: `longAvg`
1000→13500). Sync находится, но 80-битные кадры «съезжают» → `parseFrame`
бракует всё, `lastFrameMs` не обновляется → вечное моргание.

Исправлено:
- в `loop` при отсутствии фронтов > 5 мс фаза декодера сбрасывается
  (`haveEdge = false`), первый фронт после паузы — новая база без фантомного
  бита;
- фильтр правдоподобия (блок 6b): одиночный сбойный кадр (валидный по BCD, но
  с чужим TC — напр. час 02→04) не выводится;
- **сторож (блок 7b)**: если сигнал идёт (фронты свежие), а валидных кадров нет
  > 400 мс — декодер мягко реинициализируется (пороги/фаза, как при Reset).

### «Залипание» после паузы — диагноз по факту

На стенде: остановить/перезапустить таймкод → дисплей **навсегда** мигает старым
TC; при этом **Reset платы сразу возвращает нормальную работу** (сигнал в
порядке). Значит залипает не вход, а адаптивное состояние декодера
(`shortAvg`/`longAvg`/`thrShort`) — переходный процесс/шум на входе в тишине
уводит пороги, и без полного сброса декодер не восстанавливается. Сторож
эмулирует Reset автоматически (в логе — строка `REINIT`).

**Подтверждено на стенде (2026-09-21): после паузы/перезапуска сигнала новый TC
отображается нормально, без Reset платы.**

### Релизная сборка

`#define DEBUG 0` — UART-лог отключён. Flash 2986 Б (9%), RAM 215 Б (10%)
(с логом было 5168 Б / 412 Б). Для снятия лога снова поставить `DEBUG 1`:
`tools/probe_reset.py` + `tools/analyze_capture.py` читают именно D-строки.

---

## [2026-09-02] Ранняя (неполная) диагностика

### Байтовый сдвиг найден верно, tens-нибблы — нет

Стандартный SMPTE 12M LTC (LSB-first байты кадра):
`b0=кадры(+DF), b1=кадры-дес+CF, b2=СЕКУНДЫ ед., b3=СЕКУНДЫ дес.,
 b4=МИНУТЫ ед., b5=МИНУТЫ дес., b6=ЧАСЫ ед., b7=ЧАСЫ дес., b8..b9=sync`

Скетч читал секунды из b1 (user bits!), минуты из b2, часы из b3 — сдвиг на
байт был исправлен; но tens-нибблы (b3/b5/b7) по-прежнему читались неверно
(см. раздел 2026-09-21 выше).

Доказательства:
- Эталонное декодирование WAV (python, побайтово): секунды лежат в b2
  (0x1E=30 на t=150с), минуты в b4, часы в b6 (01/02/03 = номер файла).
  В b1/b3/user bits_generator кладёт свои данные (UB1≈const, UB2 ≈ десятки секунд + 1).
- Захват Nano 15 с: `E=2365/с при теории 2363, deb=0, sync=24/с` — входной
  тракт ЧИСТЫЙ, все фронты на месте. Эталон «E≈2900» из старого плана был
  для случайных данных; реальный LTC с такими файлами даёт ~23% единиц.
- «Вставки лишних битов» (03→06) = меняющиеся user bits, читавшиеся как время.
- 90–98% отбраковки = parseFrame отвергал кадры с UB > 9 (не BCD).
- «Успех стенда» 30fps@2:02 → `03:02:02:03` в верной карте сходится поле-в-поле;
  сегодняшние TC с часами 01→02→03 за 15 с — в неверной. На плате СЕЙЧАС
  прошивка с НЕВЕРНОЙ картой (текущий репозиторий), ранний верный тест шёл
  с другой (верной) сборки.
- «Мигание/потеря» дисплея и скачущие цифры сейчас = неверная карта, не шум.

### Проблема «Б» (UART) тоже закрыта — канала всегда был жив

- Рецепт `stty + cat` из старого плана ненадёжен: break-состояния сыплются
  как NUL-флуд. Правильное чтение — `tools/probe_reset.py`
  (termios raw + IGNBRK, чтение сразу после открытия): поток чистый,
  `LTC reader init OK` + TC-строки, 0 NUL.
- Флуды мусора прошлой сессии = плавающие D0/D1 в окнах сброса/пауз + 
  клиновидное состояние драйвера. В скетч добавлены `pinMode(0/1, INPUT_PULLUP)`
  до `Serial.begin` (определяет линию в паузах).
- avrdude: `resp=0x4c` — это 'L', ПЕРВЫЙ БАЙТ БАННЕРА. DTR-сброс работает,
  плата перезапускается и сразу печатает баннер → **ЗАГРУЗЧИК НЕ ОТВЕЧАЕТ**
  (нет/бит). Прошивка по UART невозможна, 115200 и 57600 бесполезны.

## Что делать дальше (по порядку)

1. **Прошивка через ISP** (единственный путь): USBasp (~$3) или вторая
   Arduino Nano как ISP-программатор. Подключение к ICSP-разъёму платы.
   ВНИМАНИЕ: hex в кэше Arduino — СТАРЫЙ (с неверными tens). Пересобрать.
   Готовый hex (пересобрать!):
   `~/Library/Caches/arduino/sketches/C417CD54F33355466EA3CF901DD1F498/ltc_reader_nano.ino.hex`
   Сборка: `"/Applications/Arduino IDE.app/Contents/Resources/app/lib/backend/resources/arduino-cli" compile --fqbn arduino:avr:nano ltc_reader_nano`
   (в hex уже: верная карта полей + яркость 0x03 + подтяжки D0/D1 + DEBUG=1).
   Через ISP же можно заодно залить загрузчик (optiboot) — тогда следующие
   прошивки пойдут по UART как обычно.
2. **Проверка по дисплею после прошивки**: WAV с начала, громкость 100% →
   дисплей должен показать истинный TC файла (24fps: 02:00:00:xx и т.д.),
   идущий в реальном времени; при остановке звука через ~0.5 с — мигание.
3. Если снова появятся артефакты — только тогда возвращаться к аналоговым
   фиксам (RC-фильтр и т.п.). Сейчас данных за шум НЕТ.

## Технические детали

- Порт: `/dev/cu.usbserial-A5069RR4`, 115200 8N1.
- Чтение: `python3 tools/probe_reset.py 115200 15 /tmp/capture.txt`
  (аргументы: скорость, секунды, файл; при открытии порта плата сбрасывается —
  баннер в начале захвата это маркер успешного чтения).
- Анализ: `python3 tools/analyze_capture.py /tmp/capture.txt`
  (сводка D-строк vs эталоны, битые строки, разрывы TC; raw теперь
  расшифровывается по верной карте b0/b2/b4/b6).
- Эталоны D-строк для ЭТИХ файлов (24fps, ~23% единиц): E≈2365, S≈886,
  L≈1479, deb=0, sync=24, thr≈781–790 тиков.
- avrdude (когда появится ISP): `... -c <isp> -p atmega328p -P <port> -b ...`
  `-U flash:w:<hex>:i`; для загрузчика: `-U lock:w:0x3F:m -U efuse:w:...` —
  проще: `"-.../arduino-cli" burn-bootloader --fqbn arduino:avr:nano
  --programmer usbasp` (или arduino_as_isp).
- Формат D-строки: `D E=<фронты/с> deb=<дребезг/с> S=<короткие/с>
  L=<длинные/с> sync=<совпадений/с> thr=<порог, тики> raw=<10 байт hex>`.
- DEBUG=1 включён; отключить диагностику — `#define DEBUG 0`.
